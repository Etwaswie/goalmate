{
  "name": "goal-decomposer-app",
  "version": "0.1.0",
  "description": "Prototype web app: user enters a goal, backend decomposes it into sub-goals (neural model pluggable).",
  "private": true,
  "scripts": {
    "dev": "node src/server.js",
    "start": "NODE_ENV=production node src/server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  }
}
